import 'package:flutter/widgets.dart';

const emptyWidget = SizedBox.shrink();

Widget marginVertical({required margin}) => SizedBox(height: margin);
Widget marginHorizontal({required margin}) => SizedBox(width: margin);
